# perf_event réservé à root (fuite d'infos side-channel via compteurs de perf).
# Impact : uniquement le profiling dev NON-root (perf, hotspot) — sudo perf reste OK.
# Source : secureblue 55-hardening.conf. Revert : hardening/reverts/revert_perf_event.sh
kernel.perf_event_paranoid=3
